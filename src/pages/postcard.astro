---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Гостевая книга — Нуриль Барадусов"
  description="Здесь вы можете оставить записку">
  <main>
    <section data-postcard>
      <h1>Гостевая книга</h1>

      <div class="content">
        <p>
          Здесь вы можете оставить записку: нарисовать, прикрепить сообщение и
          ссылку. Они публикуются после моей модерации, и только те, которые мне
          понравятся.
        </p>
      </div>

      <div class="pc-stage">
        <div class="pc-tools" role="toolbar" aria-label="Инструменты">
          <div class="pc-group">
            <button
              class="pc-btn"
              data-tool="pen"
              aria-pressed="true"
              type="button">
              Перо
            </button>
            <button
              class="pc-btn"
              data-tool="eraser"
              aria-pressed="false"
              type="button">
              Ластик
            </button>
          </div>

          <div class="pc-group" role="group" aria-label="Толщина">
            <button
              class="pc-btn pc-btn--dot"
              data-size="3"
              aria-pressed="false"
              aria-label="Тонко"
              type="button">
              <span style="--d: 5px"></span>
            </button>
            <button
              class="pc-btn pc-btn--dot"
              data-size="7"
              aria-pressed="true"
              aria-label="Средне"
              type="button">
              <span style="--d: 10px"></span>
            </button>
            <button
              class="pc-btn pc-btn--dot"
              data-size="16"
              aria-pressed="false"
              aria-label="Толсто"
              type="button">
              <span style="--d: 16px"></span>
            </button>
          </div>

          <div class="pc-group">
            <button class="pc-btn" data-act="undo" type="button">
              Отменить
            </button>
            <button class="pc-btn" data-act="redo" type="button">
              Вернуть
            </button>
            <button class="pc-btn" data-act="clear" type="button">
              Стереть
            </button>
          </div>
        </div>

        <div class="pc-card">
          <canvas
            class="pc-canvas"
            width="480"
            height="800"
            aria-label="Холст открытки">
          </canvas>
        </div>
      </div>

      <div class="pc-form">
        <label class="pc-field">
          <span>Имя или сообщение</span>
          <input class="pc-message" type="text" maxlength="70" />
        </label>

        <label class="pc-field">
          <span>Ссылка</span>
          <input
            class="pc-link"
            type="url"
            maxlength="200"
            placeholder="example.com"
          />
        </label>

        <button class="pc-btn pc-btn--primary pc-send" type="button">
          Отправить
        </button>
      </div>

      <p class="pc-status" role="status" hidden></p>

      <h2 class="pc-heading">Записки от гостей</h2>

      <p class="pc-empty" hidden></p>

      <ul class="pc-gallery"></ul>
      <template class="pc-item-tpl">
        <li class="pc-item">
          <button class="pc-item__button" type="button">
            <canvas class="pc-item__canvas" width="480" height="800"></canvas>
          </button>
          <div class="pc-item__name">
            <a
              class="pc-caption-link"
              target="_blank"
              rel="noopener noreferrer nofollow"
              hidden>
            </a>
            <span class="pc-caption-plain"></span>
            <span class="pc-caption-note" hidden></span>
          </div>
        </li>
      </template>

      <dialog class="pc-viewer">
        <div class="pc-viewer__box">
          <div class="pc-card pc-card--flat">
            <canvas class="pc-viewer__canvas" width="480" height="800"></canvas>
          </div>
          <div class="pc-viewer__bar">
            <span class="pc-viewer__who">
              <a
                class="pc-caption-link"
                target="_blank"
                rel="noopener noreferrer nofollow"
                hidden>
              </a>
              <span class="pc-caption-plain"></span>
            </span>

            <span class="pc-group">
              <button
                class="pc-btn pc-viewer__prev"
                type="button"
                aria-label="Предыдущая">
                ←
              </button>
              <button
                class="pc-btn pc-viewer__next"
                type="button"
                aria-label="Следующая">
                →
              </button>
              <button class="pc-btn pc-viewer__close" type="button">
                Закрыть
              </button>
            </span>
          </div>
        </div>
      </dialog>
    </section>
  </main>
</Layout>

<script>
  import '../scripts/postcard';
</script>

<style>
  .pc-stage {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-s);
  }

  @media (min-width: 620px) {
    .pc-stage {
      flex-direction: row;
      align-items: flex-start;
      justify-content: center;
      gap: var(--space-l);
    }

    .pc-tools {
      order: 2;
      flex-direction: column;
      gap: var(--space-s);
      width: 190px;
    }
  }

  .pc-tools {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs) var(--space-m);
  }

  .pc-group {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2xs);
  }

  .pc-field input:focus {
    outline: none;
    box-shadow: inset 0 0 0 1px var(--color-ink);
  }

  .pc-btn {
    padding: var(--space-2xs) var(--space-s);
    border: 2px solid var(--color-ink);
    background: transparent;
    color: inherit;
    font-size: var(--text-s);
    line-height: var(--leading-tight);
    cursor: pointer;
  }

  .pc-btn:hover:not(:disabled) {
    background: var(--color-hover);
  }

  .pc-btn[aria-pressed='true'] {
    background: var(--color-ink);
    color: var(--color-bg);
  }

  .pc-btn:disabled {
    opacity: 0.35;
    cursor: default;
  }

  .pc-btn--dot {
    display: grid;
    place-items: center;
    width: 40px;
    padding: var(--space-2xs);
  }

  .pc-btn--dot span {
    width: var(--d);
    height: var(--d);
    border-radius: 50%;
    background: currentColor;
  }

  .pc-btn--primary {
    background: var(--color-ink);
    color: var(--color-bg);
    padding-inline: var(--space-l);
  }

  .pc-btn--primary:hover:not(:disabled) {
    opacity: 0.85;
    background: var(--color-ink);
  }

  /* Открытка — всегда белая бумага с чёрными чернилами, независимо от темы:
     рисунки хранятся как штрихи на белом. */
  .pc-card {
    position: relative;
    flex: none;
    width: min(100%, 400px);
    background: var(--color-paper);
    border: 2px solid var(--color-ink);
    box-shadow: 7px 7px 0 var(--color-rule);
  }

  .pc-card--flat {
    width: 100%;
    box-shadow: none;
  }

  .pc-canvas,
  .pc-viewer__canvas {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 3 / 5;
    touch-action: none;
  }

  .pc-canvas {
    cursor: crosshair;
  }

  .pc-card--flying {
    position: fixed;
    z-index: 60;
    margin: 0;
    pointer-events: none;
    will-change: transform;
  }

  .pc-form {
    display: grid;
    gap: var(--space-s);
    justify-items: start;
    max-width: 420px;
  }

  .pc-field {
    display: grid;
    gap: var(--space-2xs);
    width: 100%;
  }

  .pc-field span {
    font-weight: var(--weight-bold);
    font-size: var(--text-s);
  }

  .pc-field input {
    width: 100%;
    padding: var(--space-2xs) var(--space-s);
    border: 2px solid var(--color-ink);
    background: transparent;
    color: inherit;
    font-size: var(--text-s);
  }

  .pc-status,
  .pc-empty {
    max-width: 420px;
    font-size: var(--text-s);
  }

  .pc-status[data-kind='error'] {
    padding: var(--space-2xs) var(--space-s);
    border: 2px solid var(--color-ink);
  }

  .pc-empty {
    opacity: 0.65;
  }

  .pc-gallery {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-m);
  }

  .pc-gallery > li + li {
    margin-top: 0;
  }

  @media (max-width: 600px) {
    .pc-gallery {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .pc-item {
    display: grid;
    grid-template-rows: auto 1fr;
    margin: 0;
    border: 2px solid var(--color-ink);
    background: var(--color-paper);
  }

  .pc-item:hover {
    box-shadow: 5px 5px 0 var(--color-rule);
  }

  .pc-item__button {
    display: block;
    width: 100%;
    padding: 0;
    border: 0;
    background: var(--color-paper);
    color: inherit;
    text-align: left;
    cursor: pointer;
  }

  .pc-item__canvas {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 3 / 5;
  }

  .pc-item__name {
    display: block;
    padding: var(--space-2xs) var(--space-xs);
    border-top: 2px solid var(--color-ink);
    background: var(--color-bg);
    font-size: var(--text-s);
    line-height: var(--leading-snug);
  }

  .pc-item__name::after {
    content: '\200b';
  }

  /* Записка на модерации гасится прозрачностью целиком, а не серым текстом. */
  .pc-item.is-pending {
    opacity: 0.5;
  }

  .pc-item.is-pending .pc-item__button {
    opacity: 0.38;
  }

  .pc-caption-note {
    display: block;
    font-size: var(--text-2xs);
  }

  .pc-caption-note[hidden],
  .pc-caption-link[hidden],
  .pc-viewer__site[hidden] {
    display: none;
  }

  .pc-item.is-new {
    animation: pc-land 0.34s ease-out;
  }

  @keyframes pc-land {
    from {
      transform: scale(1.07);
    }
  }

  .pc-viewer {
    margin: 0;
    width: 100vw;
    max-width: none;
    height: 100dvh;
    max-height: none;
    padding: var(--space-l) var(--space-s);
    border: 0;
    background: transparent;
    color: inherit;
    overflow: auto;
    overscroll-behavior: contain;
    display: none;
  }

  .pc-viewer[open] {
    display: flex;
    align-items: flex-start;
    justify-content: center;
  }

  .pc-viewer__box {
    margin: auto;
    width: min(92vw, 424px);
    padding: var(--space-s);
    border: 2px solid var(--color-ink);
    background: var(--color-bg);
  }

  .pc-viewer::backdrop {
    background: rgba(0, 0, 0, 0.55);
  }

  .pc-viewer__bar {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-s);
    margin-top: var(--space-s);
  }

  .pc-viewer__bar .pc-group {
    flex-wrap: nowrap;
    margin-left: auto;
  }

  .pc-viewer__who {
    flex: 1 1 auto;
    min-width: 0;
    font-size: var(--text-s);
    line-height: var(--leading-snug);
    overflow-wrap: anywhere;
  }

  @media (prefers-reduced-motion: reduce) {
    .pc-item.is-new {
      animation: none;
    }
  }
</style>
